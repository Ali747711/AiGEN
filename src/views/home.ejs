<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Gemini AI Chat</title>
  <!-- Tailwind CDN for instant beauty (or use your own CSS) -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
  <style>
    .prose code { @apply bg-gray-100 dark:bg-gray-800 px-1.5 py-0.5 rounded; }
    pre { @apply overflow-x-auto; }
  </style>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-4">

  <div class="w-full max-w-4xl bg-white rounded-2xl shadow-2xl overflow-hidden">
    <div class="bg-success p-6 text-white">
      <h1 class="text-3xl font-bold text-white text-center">Gemini AI Chat</h1>
    </div>

    <div class="p-8">
      <!-- Show AI Response -->
      <% if (responseHtml) { %>
        <div class="mb-8">
          <div class="flex items-start gap-4 mb-6">
            <div class="w-10/1"><p class="w-10 h-10 bg-gray-200 flex rounded-full items-center justify-center font-bold text-gray-600">You</p></div>
            <div class="w-10/9 bg-gray-100 rounded-lg px-5 py-3 max-w-xl">
              <%= userMessage %>
            </div>
          </div>

          <div class="flex items-start gap-4">
            <div class="w-10/1 "><p class="w-10 h-10 bg-success rounded-full flex items-center justify-center text-white font-bold">AI</p></div>
            <div class="w-10/9 prose prose-lg max-w-none dark:prose-invert">
              <%- responseHtml %>
            </div>
          </div>
        </div>
      <% } %>

      <% if (error) { %>
        <div class="text-red-500 text-center font-medium text-center mb-6"><%= error %></div>
      <% } %>

      <!-- Form -->
      <form action="/ask" method="POST" class="flex gap-3">
        <input 
          type="text" 
          name="message" 
          placeholder="Ask Gemini anything..." 
          required
          class="flex-1 w-9/6 px-5 py-4 text-lg border border-gray-300 rounded-xl focus:outline-none "
          autocomplete="off"
          value=""
        />
        <button 
          type="submit"
          class="btn btn-success w-9/3"
        >
          Send
        </button>
      </form>
    </div>
  </div>

  <!-- Optional: Syntax highlighting for code blocks -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
  <script>hljs.highlightAll();</script>
</body>
</html>